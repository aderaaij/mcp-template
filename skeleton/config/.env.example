# __MCP_NAME__ MCP configuration — copy to config/.env and fill in, then:
#   systemctl --user restart __MCP_SLUG__
#
# For production you can TPM-seal these instead of leaving plaintext here —
# see app/config.py and the project README.

# Example credential (uncomment + use in app/config.py):
# __MCP_ENV_PREFIX__API_KEY=

__MCP_ENV_PREFIX__LOG_LEVEL=INFO
